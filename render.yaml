services:
  - type: web
    name: skhovyshche-online
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    healthCheckPath: /api/ready
    envVars:
      - key: PERSISTENCE_MODE
        value: ephemeral-allowed
